# النشر على خادم البيت

الخدمة تعمل على `127.0.0.1:3900` من `/mnt/user/personal/projects/dokits`، وبياناتُها في `/mnt/user/appdata/dokits/`. النشرُ أمرٌ واحد على الخادم:

```bash
bash /mnt/user/personal/projects/dokits/deploy/deploy.sh
```

## قياساتُ أوّل نشرٍ (30 آب 2026)

| القياس | القيمة |
|---|---|
| الذاكرة أثناء العمل | **63م.ب** من سقف 200م.ب |
| حجم الصورة | 304م.ب على القرص |
| الانكشاف | `127.0.0.1` فقط — محجوبةٌ عن الشبكة المحلّية (مُثبَتٌ بالفحص) |

## دروسٌ مقيسة (وقعنا فيها فعلاً — لا تكرّرها)

**١. `npm ci` يفشل على الخادم بلا عدّة بناء.** ‏better-sqlite3 وحدةٌ أصليّة تحتاج node-gyp، وصورة `node:22-slim` بلا Python ولا مترجم. الحلّ: تثبيتُ `python3 make g++` **في مرحلة البناء وحدها** — صورةُ التشغيل تبقى نظيفة.

**٢. ‏`env_file` بمسارٍ مطلق يفشل داخل حاوية docker:cli.** هذه عائلةُ فخّ المادّة ٨ز: compose يقرأ `env_file` **بنفسه** داخل حاوية الـCLI، وما لا يُركَّب لا وجودَ له عندها — فتظهر رسالة «not found» لملفٍّ يراه المضيف بعينه. الحلّ: تركيبُ `/mnt/user/appdata/dokits` بمساره المطلق نفسه إلى جانب مسار المشروع.

**٣. سطرُ اللوحة لا يحمل عنوان Tailscale.** الخدمة مربوطةٌ بـloopback، فعنوانُ `nas.tail29eda6.ts.net:3900` **لا يعمل** (مُثبَتٌ بالقياس). العمودُ الخامس يحمل `https://dokits.net` — عنوانَها الحقيقيّ متى قام النفق.

**٤. تحذير `swap limit capabilities` عند التشغيل غيرُ ضارّ هنا.** النواةُ لا تفرض `memswap_limit`، لكنّ المضيفَ **بلا swap أصلاً** (`Swap: 0B`) — فخطرُ موت التبديل (المادّة ٨ل) منتفٍ بنيويّاً، وسقفُ الذاكرة نفسه مفروضٌ ويعمل.

## ما سُجِّل في نظام البيت

- `/var/lib/docker/unraid-autostart` ← سطر `dokits 0` (المادّة ٣أ)، والنسخةُ الاحتياطيّة في `/boot/config/scripts/unraid-autostart.bak`.
- `/mnt/user/appdata/nas-bot/dash.tsv` ← السطر 34: التطبيقات ‹ أدواتي ‹ Do Kits (بنسخةٍ احتياطيّةٍ مؤرَّخة قبل التعديل).
- الأسرار: `/mnt/user/appdata/dokits/.env` بصلاحيّة 600 — تُولَّد على الخادم ولا تدخل المستودع أبداً.

## اختبار القبول بعد أيّ تغيير (المادّة ١١)

```bash
docker restart dokits && sleep 8
curl -fsS http://127.0.0.1:3900/api/health | grep -q '"ok":true' && echo ✓
docker stats dokits --no-stream --format '{{.MemUsage}}'
```

والتحقّقُ من البقاء: التصنيفاتُ والأدواتُ ورقمُ الهجرة يجب أن تبقى كما هي بعد إعادة التشغيل.

## ربطُ النطاق (بيد المالك — لم يتمّ بعد)

النشرُ العامّ عبر نفق cloudflared القائم (شبكة host، فالوجهةُ `http://localhost:3900` مباشرةً). الترتيبُ إلزاميّ — **لا يُنشأ الاسمُ قبل أن تقوم الخدمة** (المادّة ١٢):

1. إضافة `dokits.net` منطقةً في Cloudflare.
2. تبديلُ خوادم الأسماء عند GoDaddy إلى ما تعطيه Cloudflare.
3. في النفق ← Public Hostname: ‏`dokits.net` ⟵ `http://localhost:3900`.
